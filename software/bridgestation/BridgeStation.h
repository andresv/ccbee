#ifndef BRIDGESTATION_H
#define BRIDGESTATION_H

enum {
    AM_TRANSPORT = 0x37,            // transport packet am id


    WAIT_RX_PACKET_END_TIME = 10,   // specifies how many ms are waited for the next byte until packet is sent
};

#endif
